# GitHub에 올리는 법 (GitKraken)

## 먼저 — 지난번에 왜 꼬였나

폴더 안에 저장소가 **겹겹이** 들어 있었습니다.

```
기출문제검색기 제작/.git          ← 여기도 저장소
├─ math/.git                       ← 여기도
│  └─ math/.git                    ← 여기도
└─ 기출문제_검색기/.git            ← 여기도
   └─ math/.git                    ← 여기도
      └─ math/.git                 ← 여기도
```

여섯 개가 서로 겹쳐 있었고, 그중 다섯은 **만들다 만 껍데기**였습니다
(브랜치 이름이 비어 있고 `index.lock` 이 남아 있었습니다). GitKraken이 여기 걸려 있었습니다.

지금은 전부 지웠습니다. **저장소는 `기출문제_검색기` 폴더 하나에만 만드세요.**

---

## 올리는 순서

### 1. GitHub에서 빈 저장소 만들기

<https://github.com/new> — 이름은 아무거나(예: `math`).
**README·.gitignore·라이선스는 체크하지 마세요.** 비어 있어야 합니다.

### 2. GitKraken에서 열기

`파일 → Open Repo` 가 아니라 **`Init Repo`** 입니다.

- Init Repo → Local Only
- 위치는 **`기출문제_검색기` 폴더** 를 고릅니다 (그 위 `기출문제검색기 제작` 이 아닙니다)
- 만들어지면 `.gitignore` 가 이미 있으니 그대로 둡니다

### 3. 원격 붙이기

왼쪽 `REMOTE` 옆 `+` → GitHub 저장소 주소를 넣습니다.

### 4. 커밋하고 올리기

오른쪽 목록에서 `Stage all changes` → 메시지 적고 `Commit` → 위쪽 `Push`.

**처음 올릴 때 5,400개 파일 · 150MB라 몇 분 걸립니다.** 중간에 끊지 마세요.

---

## 알아두실 점

**`b64` 폴더는 올리지 않습니다.** `.gitignore` 에 넣어 두었습니다.
180MB나 되는데, **웹 주소로 열면 필요 없는 파일**입니다.
(컴퓨터에서 파일을 직접 더블클릭해 열 때만 «클릭 복사» 기능에 쓰입니다.
웹에서는 브라우저가 `img` 폴더에서 바로 읽습니다.)

**GitHub Pages로 공개하려면** 저장소 → Settings → Pages →
Source를 `Deploy from a branch`, 브랜치를 `main` / `/(root)` 로 두면 됩니다.
몇 분 뒤 `https://아이디.github.io/저장소이름/` 으로 열립니다.

**한 파일이 100MB를 넘으면 GitHub가 거절합니다.** 지금은 가장 큰 파일이
`data/sim.js` 1.3MB라 문제없습니다.

**`COLLECT_URL` 을 확인하세요.** `index.html` 위쪽에 구글 시트 주소가 들어 있어야
응답이 모입니다. 새 파일로 바꾸실 때마다 확인해 주세요.

**저작권** — 문항은 한국교육과정평가원과 시·도교육청에 저작권이 있습니다.
저장소를 공개(Public)로 두실지 비공개(Private)로 두실지 한 번 더 생각해 주세요.
비공개로 두면 GitHub Pages는 유료 요금제에서만 됩니다.
