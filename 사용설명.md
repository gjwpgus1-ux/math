# 수학 수능/모평/학평 기출문제 검색기 사용설명

`index.html`을 더블클릭하면 열립니다. 설치나 인터넷 연결이 필요 없습니다.

시험 266개 · 문항 5,390개 (수능·모의평가 / 고1·고2·고3 전국연합)

---

## 검색하는 법

검색창 하나에 **문구·연도·시행·과목·학년·문항번호를 섞어서** 쓰면 됩니다.
타이핑을 멈추면 결과가 바로 나옵니다. 띄어쓰기는 무시하고 찾습니다.

| 이렇게 쓰면 | 이런 결과 |
|---|---|
| `둘러싸인 부분의 넓이` | 그 문구가 들어간 모든 문항 |
| `고2 6월 14번` | 연도별 고2 6월 14번 모음 |
| `24학년도 수능 14번` | 그 한 문항 (학년도 그대로) |
| `2025년 수능` | 2025년에 친 26학년도 수능 **+** 25학년도 수능 (둘 다) |
| `확통 정규분포` | 확률과 통계 중 정규분포가 나오는 문항 |
| `2020년 4월 고3 나형 29번` | 그 한 문항 |
| `25 고3 3월 1,2,3,7,10,30` | 그 시험의 1·2·3·7·10·30번만 |
| `고3 6월, 9월 22번` | 해마다 치른 6월·9월 모평의 22번 전부 |
| `25 고3 3월 공통 1~5` | 1번부터 5번까지 |

### 여러 개 한꺼번에

쉼표로 나열하면 그것만 골라 옵니다 — `1,2,3,7,10,30`, `6월, 9월`.
물결이나 붙임표로 범위도 됩니다 — `1~5`, `21-25`.

### 고3의 6월·9월은 평가원 모의평가입니다

고3 전국연합은 3·4·5·7·10월뿐이라, `고3 6월` `고3 9월` `고3 수능` 은 **수능·모의평가**로 알아듣습니다.

### 숫자는 연도일 수도, 달일 수도, 번호일 수도 있습니다

적으신 **순서대로** 연도 → 달 → 번호에 끼워 넣어, **말이 되는 읽기를 모두 찾아 함께 보여 줍니다.**

| 입력 | 이렇게 읽습니다 |
|---|---|
| `12 12` | 12년 12월 · 12년 12번 · 12월 12번 (셋 다) |
| `24 6 14` | 24년 6월 14번 (하나뿐) |
| `9` | 9년 · 9월 · 9번 |
| `22` | 22년 · 22번 (22월은 없으니 뺍니다) |
| `고3 6월 22` | 달이 이미 정해졌으니 22년 · 22번 |

갈래가 여럿이면 상태줄에 `22년 90개` `22번 30개` 처럼 각각 몇 개인지 뜨고,
**그 알약을 누르면 그 갈래만** 남습니다. 다시 누르면 풀립니다.

처음부터 하나로 못박고 싶으시면 **`22번` `25년` `6월` 처럼 단위를 붙이면** 됩니다.

### 연도는 두 가지로 부를 수 있습니다

| 이렇게 쓰면 | 찾는 것 |
|---|---|
| `26학년도` | **학년도 그대로.** 26학년도 수능·9월·6월 모평 |
| `2025년` · `25년` | **학년도와 실시 연도 둘 다.** 2025년에 친 26학년도 시험 + 25학년도 시험 |

헷갈리실 때는 **`학년도`를 붙이면 정확히 그 학년도만** 나옵니다.

- **띄어쓰기는 무시**합니다. `둘러싸인부분의넓이`도 똑같이 찾습니다.
- 문구 그대로 없으면 자동으로 *낱말을 모두 포함*하는 검색으로 넘어갑니다.
- 과목은 `공통` `확통` `확률과 통계` `미적` `미적분` `기하` `가형` `나형` 모두 알아듣습니다.
- Enter를 누르면 검색기록 탭에 남습니다. 탭을 누르면 그 검색으로 돌아갑니다.

**주의 — 개념어는 검색되지 않습니다.** 문제지에 인쇄된 글자만 찾기 때문에,
`삼각함수`나 `조건부확률` 같은 단원명은 결과가 없습니다(문제엔 `sinθ`만 있으니까요).
`정규분포` `등비수열` `내분하는 점`처럼 **문제에 실제로 적히는 말**로 찾으세요.

---

## 두 가지 모드

제목 옆의 `문항검색모드` / `학습모드` 단추로 바꿉니다. 고른 모드는 다음에 열어도 그대로입니다.
그 오른쪽의 `유사문항 비교` 와 `성취기준 달기` 는 언제든 눌러 열 수 있습니다.

**보는 방식은 두 모드가 같습니다.** 한 화면에 **4문항이 가로로 나란히** 보이고, 스크롤 없이 넘겨 봅니다.

- 화면 **양 끝의 ‹ › 화살표**나 아래쪽 `이전` `다음`으로 넘깁니다.
- 키보드 **← →** 로도 넘어갑니다. 검색창에 커서가 있을 땐 글자 이동이 우선이니,
  화살표 단추를 한 번 누른 뒤부터 키보드가 듣습니다.
- 지금 몇 쪽인지 아래에 `3 / 16` 처럼 표시됩니다.

### 문항검색 모드 — 훑어볼 때

- 군더더기 없이 문항만 봅니다. **그림을 클릭하면 복사**됩니다.

### 학습 모드 — 뽑아서 인쇄할 때

- 각 문항에 **체크상자**가 생깁니다. **그림을 클릭하면 선택/해제**되니
  작은 체크상자를 겨냥할 필요가 없습니다.
  (이 모드에서 복사하시려면 문항 위쪽의 `복사` 단추를 쓰세요.)
- **선택은 쪽을 넘겨도 그대로 남습니다.** 여러 쪽을 돌며 고른 뒤 한꺼번에 인쇄하시면 됩니다.
  도구줄의 `선택 n개` 는 지금 화면이 아니라 **고른 전부**를 셉니다.
- `전체 선택` / `전체 해제` 는 보이는 4개가 아니라 **지금 걸러진 문항 전부**를 다룹니다.
- 고른 문항은 `학습지 인쇄` 또는 `오답노트 인쇄` 로 넘어갑니다.

---

## 문항 다루기

**이미지를 클릭했을 때 하는 일이 모드마다 다릅니다.**

| 모드 | 그림 클릭 | 복사하려면 |
|---|---|---|
| 문항검색모드 | **복사** (한글·워드에 바로 붙여넣기) | 그림 클릭 |
| 학습모드 | **선택 / 해제** | `복사` 단추 |

- **저장** → 그 문항을 PNG 파일로 내려받습니다.

---

## 필터

검색창 아래 줄에 있습니다.

줄 오른쪽 끝에는 **← → 키로 다음 4문항을 볼 수 있다**는 안내가 늘 떠 있습니다.

**`필터 열기` / `필터 접기`** — 누르면 아래 네 갈래가 펼쳐집니다.

| 갈래 | 고를 수 있는 것 |
|---|---|
| 구분 | 수능 · 9월모평 · 6월모평 · 고1전국 · 고2전국 · 고3전국 |
| 시행 | 3월 · 4월 · 5월 · 6월 · 7월 · 9월 · 10월 · 11월 |
| 과목 | 공통 · 확률과 통계 · 미적분 · 기하 · 가형 · 나형 |
| 연도 | 2016 ~ 2025 (**실제로 시험을 친 해**) |

**연도는 «학년도»가 아니라 실제로 시험을 친 해입니다.**
26학년도 수능·9월·6월 모평은 모두 2025년에 치렀으므로 **2025**로 걸러집니다.
전국연합은 원래 실시 연도로 부르니 달라지는 게 없습니다.

평가원 시험은 **구분**에서 수능·9월모평·6월모평으로 갈라 고릅니다. 그래서 **시행**에는 수능이 없고
3월부터 차례대로만 놓았습니다. 시행별 문항 수는 3월 950 · 4월 318 · 5월 92 · 6월 1,070 ·
7월 410 · 9월 1,070 · 10월 530 · 11월 420 입니다.

같은 갈래 안에서 여러 개를 켜면 **그중 아무거나**, 갈래가 다르면 **모두 만족**하는 문항이 남습니다.

**`범위` — 접어도 늘 보입니다.** 필터 단추 오른쪽에 붙어 있어, 필터를 접어 둔 채로도 바로 누를 수 있습니다.

| 칩 | 해당하는 시험 |
|---|---|
| 중학 | 고1 3월 |
| 고1 | 3월을 뺀 고1 ~ 고2 3월 |
| 고2·3 | 그 밖의 모든 시험 (고2 6월 이후·고3·수능모평) |

아이들에게 낼 문제를 고를 때, 아직 안 배운 단원이 섞이지 않게 막아 줍니다.
유사문항 추천에서도 같은 기준으로 갈라 보여줍니다.

켠 필터는 단추 옆에 글자로 요약되고, `필터 해제` 로 한 번에 끕니다.

---

## 비슷한 문항 찾기

문항 카드의 **비슷한 문항** 버튼을 누르면 닮은 문항이 순서대로 나옵니다.

문제지에 쓰인 **문장 표현이 얼마나 겹치는지**를 재고, 여기에 같은 과목·같은 배점·
비슷한 문항 번호인지를 조금씩 얹어 순위를 매깁니다.

- **잘 맞는 유형** — 도형·그림 문항, 확률밀도함수, 입체도형 단면, 중복조합 나누어주기처럼
  문장이 정형화된 문항은 거의 정확히 같은 유형을 찾아 줍니다.
- **약한 유형** — "다음 조건을 만족시킨다" 같은 상투구만 겹치는 대수 문항은 헐겁습니다.
  수식이 색인에 안 들어가기 때문입니다.

**비슷한 문항** 버튼을 누르면 화면 위에 초록색 **탭**이 생깁니다.
탭을 눌러 오갈 수 있고, 여러 문항의 추천을 동시에 띄워 놓고 비교할 수 있습니다.

### 출제 범위 세 갈래

추천은 **출제 범위**로 나눠 보여줍니다.

| 범위 | 어디까지 |
|---|---|
| **중학** | 고1 3월 |
| **고1** | 고1(3월 제외) · 고2 3월 |
| **고2·3** | 나머지 (고2 6월 이후 · 고3 · 수능/모평) |

**같은 범위를 먼저** 놓고, 그 뒤에 다른 범위를 붙입니다.
다른 범위 문항에는 주황색 `고1 범위` 같은 표시가 붙으니 구분됩니다.

> 범위를 아예 막지 않은 이유가 있습니다. 중학 범위(고1 3월)는 문항이 240개뿐이라
> 같은 범위 후보가 **평균 5개**, 31문항은 **0개**입니다. 완전히 막으면 화면이 비어 버립니다.

각 묶음 안에서는 **난이도 순**(배점 → 문항번호)으로 정렬됩니다.

> 오답률 자료가 생기면 `data\rate.js` 파일로 넣어 두면 그 값이 배점 다음 기준으로 쓰입니다.
> 자료가 준비되면 알려 주세요. 파일을 만들어 드리겠습니다.

추천이 엉뚱하면 그 문항의 **유사하지 않아요** 버튼을 누르세요.

**한 사람의 의견만으로는 바로 빼지 않습니다.** 잘못된 제외가 굳어지면 되돌리기 어렵기 때문입니다.
표를 던지면 그 문항이 흐려지고 `제외 의견 1/2` 표시가 붙고,
**서로 다른 두 사람**의 의견이 모이면 그때 추천에서 빠집니다.
(같은 사람이 여러 번 눌러도 한 표로만 셉니다.)

이 약한 부분을 사람의 판단으로 메우는 것이 아래 기능입니다.

---

## 유사문항 비교 (추천을 똑똑하게 만들기)

**앱을 열면 이 화면이 먼저 뜹니다. 딱 한 문제만 답하면 닫힙니다.**
그 브라우저를 닫기 전까지 다시 묻지 않습니다. 10초면 끝납니다.

**«잘 모르겠음»으로 답하면 다른 것으로 한 번 더 묻습니다.** 비교를 모르겠다고 하면 성취기준을,
성취기준을 모르겠다고 하면 비교를 냅니다. 둘 중 **아무거나 하나만 제대로 답하면** 통과입니다.
계속 모르겠다고 하셔도 다섯 번째에는 그냥 보내 드립니다.
(`index.html` 위쪽 `var GATE_QUOTA = 1;` 로 건수를, `var GATE_MAX = 5;` 로 한도를 고칩니다. `0`이면 묻지 않습니다.)

### 화면 보는 법

```
┌──────────┬──────────┬──────────┬────────────────────┐
│          │  후보 1   │  후보 2   │ 기준 문항과 비슷한   │
│  기준 문항 │  [그림]   │  [그림]   │ 것을 고르세요        │
│  [그림]   │          │          │ ─────────────      │
│          │          │          │ [ 확정 ]           │
│          │          │          │ [ 잘 모르겠음 ]     │
└──────────┴──────────┴──────────┴────────────────────┘
```

질문과 안내 문구는 **오른쪽 파란 판**에 모아 두었습니다.
문항 그림은 화면 높이의 40%까지만 차지합니다.

**후보 문항을 눌러 고릅니다.** 고른 것은 살짝 커지고 테두리가 파랗게 바뀝니다.
**다시 누르면 취소**되고, **둘 다 골라도 됩니다.**

무엇을 골랐느냐에 따라 확정 단추의 글자가 바뀝니다.

| 고른 것 | 확정 단추 | 기록되는 답 |
|---|---|---|
| 1번만 | 이것으로 확정 | 왼쪽이 더 비슷 |
| 2번만 | 이것으로 확정 | 오른쪽이 더 비슷 |
| 둘 다 | 둘 다 비슷으로 확정 | 둘 다 비슷 |
| 아무것도 안 고름 | 둘 다 다름으로 확정 | 둘 다 다름 |

키보드로는 `1` `2` 로 고르고 `Enter` 로 확정, `0` 은 잘 모르겠음, `Space` 는 건너뛰기입니다.

**이름은 묻지 않습니다.** 브라우저마다 익명 번호(`u3k9xz` 같은)를 하나 만들어
응답자 수를 세는 데만 씁니다. 누가 답했는지는 알 수 없습니다.

**아무 쌍이나 묻지 않습니다.** 추천기가 우열을 가리기 어려워하는, 점수가 거의 같은 두 후보를
골라 보여줍니다(무작위보다 점수 차가 약 6배 작은 쌍). 답 한 건의 가치가 그만큼 큽니다.

**같은 쌍을 여러 사람에게 묻습니다 — 공통 검증 세트.**
본인이 이미 답한 쌍은 다시 나오지 않지만, 사람이 다르면 이야기가 다릅니다.
5,390문항에서 만들 수 있는 후보쌍이 20만 개가 넘어 그냥 무작위로 뽑으면
두 사람이 같은 쌍을 만날 일이 사실상 없습니다. 그래서 **모두에게 공통으로 나오는
59쌍짜리 세트**를 따로 두고, 문제를 낼 때 **약 3분의 1을 여기서** 꺼냅니다.

| 상황 | 두 명 이상이 답한 쌍 | 세 명 이상 |
|---|---|---|
| 5명 × 20건 | 8쌍 | 1쌍 |
| 10명 × 30건 | 32쌍 | 15쌍 |

같은 쌍에 답이 갈리면 그건 사람마다 '비슷하다'의 기준이 다르다는 뜻이라,
가중치를 맞출 때 그 신호를 얼마나 믿을지 정하는 근거가 됩니다.
비율과 세트 크기는 `index.html` 위쪽 `var CHECK_RATIO = 0.35;`
`var CHECK_STEP = 90;`(숫자가 작을수록 세트가 커짐)에서 조절합니다.

**목표는 300건쯤입니다.** 처음부터 학습시키는 게 아니라 이미 만들어 둔 추천 점수의
가중치(과목·배점·번호를 얼마나 볼지)를 다듬는 용도라, 이 정도면 의미가 있습니다.
관리자 메뉴 `비교데이터` 탭에서 진행 상황과 응답 일관성을 볼 수 있고,
CSV로 받아 알려 주시면 가중치를 다시 맞춰 드립니다.

### 응답이 모이는 곳 — 꼭 읽어 주세요

`COLLECT_URL` 을 넣지 않으면 응답은 **답한 사람의 브라우저 안에만** 남습니다.
학생이나 동료 선생님이 GitHub 주소에서 답해도 선생님 화면에는 오지 않고,
브라우저를 정리하거나 컴퓨터를 바꾸면 사라집니다.

**앱 폴더의 `_수집서버 \ 설치 방법.md`** 를 따라 구글 시트를 한 번 붙여 두시면
누가 어디서 답하든 시트에 한 줄씩 쌓이고, 앱을 열 때마다 **다른 분들의 응답까지 받아와**
추천이 함께 좋아집니다. 10분이면 됩니다.

| | 안 붙였을 때 | 붙였을 때 |
|---|---|---|
| 답한 것이 가는 곳 | 그 사람 브라우저 | 선생님 구글 시트 |
| 다른 사람 응답 | 못 봄 | 앱을 열면 자동으로 합쳐짐 |
| `index.html` 을 새로 올리면 | 그 브라우저 것만 남음 | 시트에 그대로 |
| 인터넷이 끊기면 | — | 대기열에 뒀다 나중에 다시 보냄 |

**응답을 지우면 시트에서도 지워집니다.** 관리자에서 `이 응답 삭제`를 누르면 시트의 그 줄이 사라지고,
다시 받아와도 되살아나지 않습니다. 다만 `이 브라우저에서 비우기`는 이름 그대로 이 브라우저만
비우는 것이라 시트는 그대로입니다.

진행 상황은 관리자 메뉴 **`수집 상태`** 탭에서 봅니다 — 보낸 건수, 못 보낸 건수,
`지금 보내기`, `시트에서 받아오기`, `파일로 받아 두기`.

현재 이 파일에는 주소가 **이미 들어 있습니다**
(`script.google.com/macros/s/AKfycbyG…/exec`). 그대로 쓰시면 됩니다.

> **앞으로 새 파일을 받으시면 주소가 그대로인지 확인해 주세요.** 응답은 시트에 있어 안전하지만,
> 주소는 파일 안에 적는 값이라 빠뜨리면 다시 브라우저에만 쌓입니다.

---

## 성취기준 달기

**2022 개정 수학과 교육과정**의 성취기준 **173개**를 싣고 있습니다.
수능·모평·학평 출제 범위가 되는 과목만 넣었습니다 — 중학교 수학, 공통수학1·2, 대수,
미적분Ⅰ·Ⅱ, 기하, 확률과 통계. 기본수학·직무수학처럼 출제된 적 없는 과목은 뺐습니다.

문항 하나를 띄우고 «어느 성취기준에 해당하나요?» 를 묻습니다. **여러 개를 고를 수 있습니다.**

### 문항에 따라 고를 수 있는 범위가 다릅니다

| 문항 | 고를 수 있는 성취기준 |
|---|---|
| **고1 3월** (중학 범위) | 중학교 수학 43개만 |
| 그 밖의 모든 문항 | 고등학교 7과목 130개만 |

고3 문항에 중학교 성취기준이 뜨는 일은 없습니다. 검색 결과도 같은 범위 안에서만 나옵니다.

```
┌────────────────┬──────────────────────────────────┐
│                │ 이 문항의 성취기준을 고르세요        │
│  문항 (화면 40%) │ 함께 만들어가는 AI 유사문항 추천…   │
│                │ ────────────────────────       │
│  [그림]         │ [1] 12대수03-03 · 대수 · 수열     │
│                │ [2] [3] [4] …                   │
│                │ [없으면 여기서 찾기]               │
│                │ [저장] [잘 모르겠음] [해당 없음]    │
└────────────────┴──────────────────────────────────┘
```

왼쪽이 문항, 오른쪽 한 단에 **질문 · 후보 4개 · 검색 · 단추**가 모두 들어 있습니다.
성취기준 문장은 15px로 키웠습니다.

### 후보는 세 갈래에서 옵니다

1. **비슷한 문항에서 물려받은 것** — 노란 테두리로 «비슷한 문항 3개가 이 성취기준입니다» 라고 적힙니다.
2. **문항에 나온 말로 찾은 것** — 어떤 낱말이 걸렸는지 아래에 적힙니다.
3. **비슷한 문항에서 빌려온 것** — 그 문항에 글자가 거의 없을 때만 씁니다.

맞는 게 없으면 아래 검색창에서 찾습니다. `등차수열` `정규분포` 같은 말도, `12대수03` 같은 코드도 됩니다.

| 키 | 하는 일 |
|---|---|
| `1`~`4` | 그 후보를 켜고 끕니다 (여러 개 가능) |
| `Enter` | 고른 것을 저장 |
| `0` | 잘 모르겠음 |
| `Space` | 다른 문항으로 |

고른 성취기준은 아래에 알약 모양으로 모이고 `×` 로 하나씩 뺄 수 있습니다.

### 응답이 쌓이면 일어나는 일

| 단계 | 하는 일 |
|---|---|
| 1명이 답함 | 잠정. 아직 아무것도 바꾸지 않습니다 |
| **2명 이상이 같게 답함** | **확정**. 성취기준마다 따로 세므로 한 문항에 확정이 여럿일 수 있습니다 |
| 확정된 문항의 이웃 | 같은 성취기준일 것으로 보고 초안 맨 앞에 올립니다 |
| 이웃이 다른 성취기준으로 확정됨 | 두 문항의 **유사도를 깎습니다** |

두 문항의 확정 성취기준 **묶음끼리** 견줍니다. 겹치는 게 하나라도 있으면 «같음»으로 봅니다.

| 두 문항의 관계 | 유사도 점수 |
|---|---|
| 같은 성취기준이 하나라도 겹침 | **+0.30** |
| 겹치진 않지만 같은 영역 | +0.05 |
| 같은 과목, 다른 영역 | −0.10 |
| 과목이 아예 다름 | **−0.50** |

### 비교 응답도 추천을 움직입니다

«왼쪽이 더 비슷» 같은 답 한 표가 그 두 문항의 추천 점수를 **±0.05** 움직입니다.
표가 아무리 쌓여도 **±0.20**을 넘지 않게 막아 두었습니다 — 한두 사람 의견으로
난이도 순서가 뒤집히지 않게 하려는 것입니다. (성취기준이 같을 때의 +0.30보다 작습니다)

| 답 | 그 쌍에 주는 값 |
|---|---|
| 왼쪽이 더 비슷 | 왼쪽 **+1표** · 오른쪽 **−1표** |
| 둘 다 비슷 | 둘 다 +0.5표 |
| 둘 다 다름 | 둘 다 −0.5표 |
| 잘 모르겠음 | 아무 영향 없음 |

«유사하지 않아요»도 이제 **구글 시트로 함께 모입니다.** 예전에는 누른 사람 브라우저에만
남아서, `NEG_THRESHOLD = 2`(두 명 이상 동의해야 제외)가 사실상 작동하지 않았습니다.

**한쪽이라도 확정되지 않았으면 아무것도 건드리지 않습니다.** 응답이 없는 동안은 지금과 똑같이 동작합니다.
가감 폭은 `index.html` 위쪽 `var WSTD = {...}`, 확정 기준은 `var STD_CONFIRM = 2;` 에서 고칩니다.

첫 화면의 의무 응답은 **한 건**이며, 잘 모르겠다고 하시면 비교와 성취기준을 번갈아 냅니다.

> 단원별 문항 분류 화면은 아직 넣지 않았습니다. 확정된 문항이 충분히 쌓이면 그때 붙이면 됩니다.
> 관리자 메뉴 `성취기준` 탭의 `확정 목록 CSV로 저장` 으로 지금 상태를 언제든 받아 볼 수 있습니다.

---

## 인쇄 양식

학습 모드에서 문항을 체크한 뒤 씁니다. 둘 다 **A4를 반으로 가르는 세로선**이 들어갑니다.

두 양식 모두 **문항 맨 위(년·월·학년·번호 표시보다도 위)에 풀이 횟수 칸 3개**가 붙어 있고,
**아래쪽 가운데에 쪽 번호**(`3 / 12`)가 찍힙니다.

칸 안에는 옅은 글씨로 `1회` `2회` `3회` 가 적혀 있습니다. 그 문제를 풀 때마다 해당 칸에 O를 치면
몇 번째 도전인지 한눈에 보입니다. 세 칸은 간격 없이 붙어 있습니다.

```
┌────┬────┬────┐
│ 1회 │ 2회 │ 3회 │
└────┴────┴────┘
 2026년 6월 고3 공통 14번
 [문항 이미지]
```

### 학습지 인쇄

문제를 풀리는 용도입니다. 배점에 따라 자리를 다르게 잡습니다.

| 배점 | 배치 |
|---|---|
| 2점 · 3점 | 한 단에 **2문항** |
| 4점 | 한 단에 **1문항** (긴 문제라 단을 다 씁니다) |

한 장에 단이 둘이니 2·3점만 있으면 **한 장에 4문항**, 4점만 있으면 **한 장에 2문항**이 됩니다.
섞여 있으면 알아서 채웁니다. 문항은 난이도 순으로 놓입니다.

**섞였을 때 자리를 어떻게 채우나** — 한 단에 한 칸이 남았는데 다음이 4점이면,
뒤쪽에서 2·3점을 하나 당겨 와 그 자리를 채웁니다. 그래야 2·3점 하나가
한 단을 통째로 차지해 크게 벌어지는 일이 없습니다. 마지막 단이 반만 차면
아래 반은 비워 둡니다.

**배점을 못 읽는 문항이 6%쯤 있습니다.** 문제지에 배점이 그림으로 박혀 있어
글자로 남지 않은 경우입니다. 이런 문항은 **그림이 세로로 얼마나 긴지**로 자리를 정합니다.
자리를 잡는 데엔 그게 실제 기준이라 결과는 거의 같습니다.

**학습지와 오답노트 모두, 쪽 아래 오른쪽에 이 문구가 찍힙니다.**

> 공교육을 위해 제작했습니다. 영리적인 목적을 위하여 사용을 금합니다.

문구를 바꾸시려면 `index.html` 의 `var NOTICE = '...'` 안의 글만 고치세요.

### 오답노트 인쇄

**한 장에 한 문항**입니다. 학습 모드 도구줄의 **`💡 오답노트 팁`** 을 누르면
빨리 만드는 요령 아홉 가지가 팝업으로 뜹니다.
(문구를 고치시려면 `index.html` 의 `var TIPS=[` 아래 글만 바꾸시면 됩니다.)

```
┌─────────────────┬─────────────────┐
│ 1회│2회│3회       │ 어느 지점에서     │
│ 26학년도 수능 14번 │ 막혔고, 그때      │
│                 │ 무엇을 시도했는가  │
│   [문항 이미지]   │ ───────────     │
│                 │ ───────────     │
│                 │                 │
│                 │ 다음에 같은 유형을 │
│                 │ 만나면 무엇부터   │
│                 │ 할 것인가        │
│                 │ ───────────     │
│                 │                 │
│                 │      3 / 12     │
└─────────────────┴─────────────────┘
```

두 질문 아래에 필기용 줄이 각각 7줄·8줄 들어갑니다.

> 인쇄 창에서 **배경 그래픽**을 켜야 가운데 구분선과 필기 줄이 나옵니다.
> 크롬은 `설정 더보기` → `배경 그래픽`에 체크하세요.

---

## 오류 제보

문항 카드의 **오류제보** 버튼을 누르면 유형과 메모를 남길 수 있습니다.
**이름은 묻지 않습니다** — 비교 화면과 같은 익명 번호로만 기록됩니다.
제보된 문항은 카드 테두리가 붉게 바뀌고 `제보 1` 표시가 붙습니다.
쌓인 제보는 관리자 메뉴에서 한 번에 볼 수 있습니다.

제보는 **구글 시트의 `오류제보` 탭으로 모입니다.** 누가 어디서 제보하든 한곳에 쌓이고,
앱을 열면 다른 분들의 제보도 받아와 관리자 화면에 함께 보입니다.
`처리완료` 표시와 삭제도 시트에 반영되므로, 컴퓨터를 바꿔도 어디까지 고쳤는지 남습니다.

잘못 잘린 문항을 고치실 때는 시트의 `오류제보` 탭을 CSV로 받으시면 됩니다.
`파일` 칸(`suneung/040_06.png`)만 있으면 원본 PDF에서 다시 잘라 낼 수 있습니다.

---

## 관리자 모드

제목 **「기출문제 검색기」를 세 번 클릭**하면 비밀번호 창이 뜹니다.

> 비밀번호: **gich2026**
> 바꾸시려면 `index.html` 을 메모장으로 열어 위쪽의 `var ADMIN_PW = 'gich2026';` 부분만 고치세요.

들어가면 상단에 `관리자` 버튼이 생기고, 미처리 제보 건수가 빨간 숫자로 표시됩니다.
관리자 화면은 **화면 전체**로 열립니다. `닫기`를 누르면 원래 화면으로 돌아옵니다.

**오류제보 탭**

- 제보 목록을 보고 `문항 보기`로 해당 문항만 띄우거나, 바로 `이미지 교체`
- `처리완료` 표시, `삭제`, `제보 목록 CSV로 저장`

**비교데이터 탭**

- 모인 응답 수, 응답자별 건수, 고른 답의 분포
- 같은 쌍이 다시 나왔을 때의 **응답 일치율** (데이터 신뢰도 확인용)
- 그중 **공통 검증 세트**에서 나온 건수
- 최근 응답 20건 — **기준·왼쪽·오른쪽 문항 그림이 클릭 없이 곧바로 가로로 나란히** 놓입니다.
  고른 쪽에는 파란 테두리와 `← 고른 쪽` 표시가 붙어, 눈으로 훑으며 판단할 수 있습니다.
- **잘못 눌렀거나 장난으로 답한 것 같으면 `이 응답 삭제`** — 그 한 건만 지워지고
  통계가 바로 다시 계산됩니다. (되돌릴 수 없습니다)
- `비교 응답 CSV로 저장` — 검증쌍 여부가 열로 함께 나갑니다

**성취기준 탭**

- 응답 수, 응답자별 건수, **확정된 문항 수**, 둘 이상 고른 응답 수, 확정 문항의 과목별 분포
- 유사도가 깎인 쌍이 몇 개인지
- 최근 응답 20건 — 문항 그림과 고른 성취기준을 나란히. 이상하면 `이 응답 삭제`
- `성취기준 응답 CSV로 저장` (성취기준마다 한 줄) · `확정 목록 CSV로 저장`

**수집 상태 탭**

- 구글 시트로 보낸 건수와 **못 보낸 건수**, 지운 응답 수, 마지막으로 받아온 때
- `지금 보내기` · `시트에서 받아오기` · `파일로 받아 두기`(만일을 대비한 통째 백업)
- 주소를 아직 안 넣으셨으면 넣는 방법이 여기에 안내됩니다

**교체한 이미지 탭**

- 지금까지 바꿔 끼운 그림 목록과 미리보기
- `원래대로` 되돌리기
- `PNG 저장` — 폴더에 영구 반영할 때 씁니다

### 이미지 교체하기 (관리자 모드)

1. 잘린 문항을 직접 캡처해 PNG나 JPG로 저장
2. 그 문항 카드의 **이미지 교체** 버튼 → 파일 선택
3. 끝. 화면·복사·저장·인쇄에 **즉시** 반영되고, 그 문항의 제보도 처리완료로 넘어갑니다

교체한 그림은 **이 브라우저 안에** 저장됩니다. 폴더를 다른 컴퓨터로 옮기거나
브라우저 데이터를 지우면 사라지므로, 오래 두려면 아래 방법으로 폴더에 넣으세요.

---

## 폴더에 영구 반영하기

문항 이미지는 PDF에서 자동으로 잘라낸 것이라, 드물게 어긋난 것이 있을 수 있습니다.
직접 만든 그림으로 바꿔 끼울 수 있습니다.

### 1. 바꿀 파일 찾기

검색 결과 카드의 **오른쪽 위에 회색 글씨로 파일 경로**가 적혀 있습니다.
예: `suneung/000_07.png` → `img\suneung\000_07.png`

관리자 모드에서 이미 교체하셨다면, `교체한 이미지` 탭의 `PNG 저장`을 누르면
알맞은 파일 이름으로 내려받아집니다.

### 2. 파일 바꾸기

그 자리에 **같은 이름의 PNG**를 덮어씁니다. (파일 이름과 확장자를 그대로 두세요.)
크기는 달라도 괜찮습니다. 화면 폭에 맞춰 자동으로 조절됩니다.

### 3. `_빌드도구 \ 이미지 바꾼 뒤 실행.bat` 을 더블클릭

이걸 하지 않으면 **화면·저장·인쇄에는 새 그림이 나오지만, 클릭 복사만 옛날 그림**이 나옵니다.
브라우저가 보안상 로컬 이미지 파일을 직접 읽지 못해서, 복사용 데이터를 따로 두기 때문입니다.

- 바뀐 이미지가 있는 시험만 골라서 갱신하므로 보통 몇 초면 끝납니다.
- 파이썬이 설치돼 있다면 `_빌드도구 \ refresh_b64.py` 를 대신 써도 됩니다.

### 4. 브라우저에서 Ctrl+F5 로 새로고침

---

## 폴더 구성

```
기출문제_검색기\
├─ index.html        ← 이 파일을 더블클릭
├─ data\index.js     ← 검색 색인 (문항 텍스트·시험 정보)
├─ data\sim.js       ← 비슷한 문항 후보 (미리 계산해 둔 것)
├─ data\std.js       ← 2022 개정 성취기준 173개
├─ img\              ← 문항 이미지 5,390개  (여기를 갈아끼웁니다)
├─ b64\              ← 클릭 복사용 데이터
├─ _수집서버\         ← 구글 시트로 응답 모으기 (Code.gs · 설치 방법.md)
└─ _빌드도구\         ← 이미지 바꾼 뒤 실행.bat, 제작 스크립트, 자동 점검
```

폴더째 복사해 USB나 다른 컴퓨터로 옮겨도 그대로 동작합니다.

---

## 알아두실 점

- **연도 표기가 자료마다 다릅니다.** 수능·모의평가는 *학년도*(26학년도 수능),
  교육청 전국연합은 *실시 연월*(2025년 3월 고1)입니다.
  수능·모평 문항에는 카드에 **`2025년 11월 시행`** 처럼 실제로 친 날을 회색 딱지로 붙여 두었습니다.
  26학년도 수능은 2025년 11월, 26학년도 6월 모평은 2025년 6월에 치른 시험입니다.
  이 딱지는 학습지·오답노트를 인쇄할 때도 문항 이름 옆에 함께 찍힙니다.
- **스캔본 120문항**(21학년도 수능 가형·나형, 2020년 4월 고3 가형·나형)은
  PDF에 글자 정보가 없어 문항 텍스트를 사람이 읽어 옮겨 적었습니다.
  드물게 오타가 있을 수 있지만 **이미지는 원본 그대로**입니다.
- 수식·숫자는 PDF 안에서 특수 문자라 검색 색인에 들어가지 않습니다.
  한글 문장으로 찾으시면 됩니다.
