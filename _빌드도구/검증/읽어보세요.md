# 자동 점검 스크립트

앱을 가상 브라우저(jsdom)에 띄워 기능이 살아 있는지 확인합니다.
파일을 고친 뒤 돌려 보면 어디가 깨졌는지 바로 나옵니다.

```
npm install jsdom
node regress.js     # 전체 기능 훑기
node tip.js         # 오답노트 팁 팝업
```

`harness.js` 는 두 파일이 함께 쓰는 준비 코드입니다.
앱 폴더 경로가 바뀌면 `harness.js` 위쪽의 `APP` 만 고치시면 됩니다.
